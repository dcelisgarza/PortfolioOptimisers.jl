# The hand-written half of the code-health gate.
#
# Nothing in this file is measured. Every entry is a human judgement, and the three generated files
# beside it are rewritten by their scripts. ADR 0073 splits the four files on exactly that line, so
# no human paragraph shares a file a refresh overwrites.
#
# The four sections, and what each one binds:
#
#   [thresholds]        which files the scheduled job files as work. Never the pass rule.
#   [scheduled_job]     how many of them stand open at once.
#   [size]              the one number here that IS part of a pass rule. ADR 0101 says why.
#   [[unmeasured_path]] a path the gate never measures. It has no row and nothing to ratchet.
#   [[exemption]]       a real number that may stand. It binds issue candidacy alone.
#   [[dismissal]]       a JET report class that is not a defect. It binds the reviewed count.
#   [[perf_dismissal]]  a performance Finding that is not a trap. It binds the rule's count.
#   [[coverage_exemption]]  a line that no test can reach. It binds the terminal condition.
#
# An Exemption, a Dismissal, a performance Dismissal and a Coverage Exemption all cite a Rationale
# by name. Citing an
# approved Rationale is bookkeeping and any contributor may do it. Adding a `[rationale.…]` block
# is a claim that some code is correct, and it needs the maintainer.

# --- thresholds ------------------------------------------------------------
#
# ADR 0076 keeps these out of the pass rule. The ratchet stops decay, and the scheduled job drives
# improvement. A file far above every threshold is green while its number holds steady.
#
# Issue #356 moved the argument count from 5 to 10. The cliff in this repository's own distribution
# sits between 9 and 10, and at 10 each of the three metrics reports the same 21 files, which is
# what makes the scheduled job's `max(value/threshold)` ranking comparable across metrics. It was
# chosen once from the cliff. It is not a rule to re-derive, because a re-derived threshold never
# terminates.

[thresholds]
cyclomatic = 10
cognitive = 15
argcount = 10
jet_reviewed = 1
# ADR 0175. An added file enters `code_health/perf_baseline.toml` with no performance Finding that
# a Dismissal does not cover, which is the entry rule `jet_reviewed` sets for JET.
perf_reviewed = 1

# --- the size ratchet ------------------------------------------------------
#
# `code_lines` is the one number in this file that the pass rule reads. ADR 0101 states the reason:
# a per-file ratchet on a line count is the sum-shaped noise that issue #336 already rejected,
# because every added helper would turn the gate red. The threshold makes the gate quiet under
# ordinary work and strict on the files that are already large. A file's ceiling is the greater of
# this number and the number `code_health/size_baseline.toml` records for it.
#
# The cliff in this repository's own distribution of code lines per file sits between 530 and 440,
# and 500 sits in it. Above it stand fifteen files, which is the same order as the twenty-one the
# complexity thresholds report. It was chosen once from the cliff. It is not a rule to re-derive,
# because a re-derived threshold never terminates.

[size]
code_lines = 500

# --- the scheduled job -----------------------------------------------------
#
# ADR 0078 caps the OPEN count rather than the count of one run. A per-run cap is blind to
# throughput, so an unworked backlog would grow at a fixed rate. This cap is self-limiting: if five
# stand open the job files nothing, and the queue paces itself to what actually closes.

[scheduled_job]
open_queue = 5

# --- unmeasured paths ------------------------------------------------------
#
# ADR 0072 measures `src/` and `ext/`. Every other tracked `.jl` file must be named here, and a
# tracked file that is neither turns the gate red. The assertion is the point: a new top-level
# directory cannot fall through unmeasured and silent.

[[unmeasured_path]]
path = "test/"
reason = "Test code. The narrower scope of ADR 0072 replaces the whole-tree scope this map charted."

[[unmeasured_path]]
path = "examples/"
reason = "Generated notebooks and their sources. They are documentation, not library code."

[[unmeasured_path]]
path = "user_guide/"
reason = "Documentation sources, rendered into docs/src/user_guide by CI."

[[unmeasured_path]]
path = "docs/"
reason = "The documentation build's own programs."

[[unmeasured_path]]
path = "code_health/"
reason = """
The gate's own scripts. They are tooling rather than library code, so ADR 0072's scope of `src/` \
and `ext/` does not reach them, and a ratchet on the instrument would measure the instrument \
rather than the subject. They are still formatted by `FormatCheck.yml` and by the pre-commit hook \
like every other `.jl` file. This entry is the ruling ADR 0072 asks for: a new directory of \
tracked `.jl` files turns the gate red until someone rules on it, and the first run of the gate \
ruled on itself."""

[[unmeasured_path]]
path = "research/"
reason = """
Prototypes. They are the subject of the live prototypes-to-seams effort, which will rewrite or \
delete many of them, and the scheduled job files each offending file at most once ever. An issue \
against a file that is about to be deleted spends a permanent slot. A prototype is measured on the \
day it becomes source: moving a file into `src/` faces the entry test of ADR 0074, because an \
Unmeasured Path holds no row for a rename to pair with."""

# --- rationales ------------------------------------------------------------
#
# One named paragraph, cited by many entries. A new block here needs the maintainer.

[rationale.keyword-constructor-arity]
text = """
An estimator's constructor takes one argument per field. This library's house style is a keyword \
constructor over a wide configuration object, so the argument count of such a constructor is a \
count of the estimator's configuration and not a measure of the constructor's difficulty. A branch \
is extractable where an argument per field is not, so the same definition stays a candidate under \
cyclomatic and cognitive complexity. Issue #356 measured the alternatives and rejected both: \
counting positional slots alone leaves 86 of the 196 files above the old threshold of 5, and \
dropping every struct constructor leaves 87, against 101. Keyword arguments and estimator \
constructors own the extreme tail, not the bulk."""

[rationale.a-cut-into-one-cluster-is-always-valid]
text = """
`validate_k_value` walks the `k - 1` tallest nodes of the dendrogram, so at `k = 1` it walks none \
and answers `true` whatever the tree looks like. The `Integer` method of \
`optimal_number_clusters` reaches its two searches only when the stated `k` is invalid, which \
needs `k >= 2`, and its downward search runs from `k - 1` down to `1`. That search therefore \
always finds a valid count, and its distance `dl` is never zero at the decision below it. The arm \
that reads `du != 0 && dl == 0` cannot be entered. It is the symmetric partner of the arm beside \
it and is kept rather than deleted, because deleting it would rest this function on an invariant \
of another function that no signature states. Measured by the sweep of issue #468: over 160 \
dendrograms with distinct heights no count was rejected at all, and on the tied trees that do \
reject one, every other arm of the decision is reached."""

[rationale.a-signature-line-counter-the-test-job-cannot-reach]
text = """
Julia allocates the counter on the `function` line of some inner constructors to code that runs \
when the method is **defined**, not when it is called. Definition happens during precompilation, \
in a process whose coverage the test job discards, so the line reads zero however many times the \
constructor runs. Measured by the sweep of issue #513 over seven test files: the constructor's \
own body carries 196 on both of its lines while the signature line carries 0. It is an artifact \
of counter allocation and not an unreached branch, so no test can pay it. The neighbouring \
constructors show the two shapes it is not: `PartialLinearConstraint`, whose signature is one \
line, carries 215 there, and `UniverseSets`, whose signature is three, carries 67. A row here is \
therefore keyed to the one definition that shows it, never to multi-line signatures as a class."""

[rationale.a-return-after-an-unconditional-throw]
text = """
A fallback method that exists only to reject a term has one unconditional `throw` for a body, and \
keeps the `return nothing` its sibling methods end with so that all three read alike. No call can \
reach that `return`: the `throw` above it takes no branch and returns to no caller, so the line is \
unreachable by construction rather than merely untested. Julia allocates a counter to it anyway. \
Measured by the sweep of issue #515 over nine test files: `feature_target!`'s fallback carries 3 on \
its `function` line and 3 on its `throw`, and 0 on the `return` below them, while the `Pair` method \
beside it carries 51 and every other line of the file is covered. The four malformed shapes \
`test_12e_asset_sets_features.jl` drives through the grammar are what raise that 3, so the method \
itself is reached and only its last line is not. Deleting the line would buy the number by making \
one of three methods shaped unlike its siblings."""

[rationale.a-closure-inside-a-top-level-const]
text = """
A top-level `const` whose value is built by a higher-order call carries an anonymous function, and \
that closure runs once, when the module is defined. Definition happens in the precompile process, \
whose coverage the test job discards, so the test process allocates the line no counter at all. \
`CoverageTools` then reads the line as code and writes a synthetic `0` over it while it converts \
the `.cov` files into `lcov.info`, and the gate reads that `0` as a miss. No test can move it. \
Measured by the review of PR #625, piece P11, on the two `Union{map(c -> Val{c}, …)...}` \
constants of `src/05_Moments/20_Base_Regression.jl`. A targeted run of \
`test_08j_base_regression.jl` under `--code-coverage` hits all 68 executable lines of the file and \
leaves no zero, and lines 649 and 667 carry a bare `-`. `CoverageTools.process_file` over the same \
source answers `0` on exactly those two lines and `nothing` on the three neighbouring constants, \
which is the synthetic zero the CI artifact then carries as `DA:649,0` and `DA:667,0`. The union \
each line builds is nevertheless correct, which is the proof that the closure ran: \
`MinValStepwiseRegressionCriterion` is `Union{Val{:aic}, Val{:aicc}, Val{:bic}}`, \
`MaxValStepwiseRegressionCriterion` is `Union{Val{:r2}, Val{:adjr2}}`, and the file dispatches on \
both. The closure and not the `const` is the shape that draws the zero, which those three \
neighbours show: `MIN_VAL_STEPWISE_REGRESSION_CRITERIA` and `MAX_VAL_STEPWISE_REGRESSION_CRITERIA` \
hold a tuple literal, `STEPWISE_REGRESSION_CRITERIA` holds a splat of the two, none of them holds \
a function body, and none of them reaches `lcov.info`."""

[rationale.a-definition-a-file-split-moved-unchanged]
text = """
A file split moves a definition byte-for-byte into a new file, and ADR 0074's entry test reads \
the new file as an addition. The definition is not new, its number did not move, and the row that \
grandfathered it died with the file it named. Two definitions of `src/01_Base.jl` stand above a \
threshold, and the row of that file recorded both: `@define_pretty_show` drove the file maximum at \
cyc = 27 and cog = 50, and `apply_preferences!` sat under it at cyc = 26 and cog = 33. The split \
of that file into one file per concept puts each of them alone in a smaller file, so each now \
drives its own file's maximum. Neither body changed. Lowering either number is separate work on \
the definition itself: the macro carries the whole pretty-show rendering, and `apply_preferences!` \
carries one branch per key of `PREFERENCE_KEYS`."""

[rationale.an-allocation-the-loop-keeps]
text = """
ADR 0175 reads an allocation inside a `for` or `while` body as a temporary unless a keeper call \
such as `push!` or `setindex!` takes it on the same line. The parser cannot see an allocation \
bound with a plain `=` that the loop keeps: one stored into a Result or a dictionary on a later \
line, one returned by the iteration's own branch, or one rebound as the state the next iteration \
reads. Each such array is a distinct output of its iteration, and it has its own length, so one \
buffer before the loop would alias every stored copy to the last one. The ADR names this case and \
says that it takes a Dismissal. Found by issue #1312 at twelve sites: the bisection frontier of \
the hierarchical optimisers, the test-index lists of the combinatorial split, the family lists of \
the cross-sectional factor axis, the NaN weight matrices of `rebuild_weight_path`, the copies that \
`replace_prior_views` returns, and the index lists `replace_coprior_views` records per row."""

[rationale.a-slice-the-result-keeps]
text = """
A slice passed to a callee that returns it, or stores it in the Result it builds, becomes a field \
of that Result. A view would make the field a `SubArray` that aliases a local matrix, which \
changes the field's type and keeps the whole matrix alive. Found by issue #1312 at \
`cross_sectional_idiosyncratic_covariance`, which returns the variance row it is given when its \
threshold is zero."""

[rationale.a-failure-message-is-a-cold-path]
text = """
ADR 0175 leaves a cold path unread: an interpolation, a `throw`, an `error`, or a logging macro. \
A function that only builds the text of a failure message is the same path in a definition of its \
own, and runs once, on the solve that failed. Its loop sorts the keys of each trial's dictionary, \
whose key type differs between trials, so a shared buffer would need an `Any` element type."""

# --- exemptions ------------------------------------------------------------
#
# Keyed (path, definition, metric), dropped before the file's maximum is taken. An Exemption binds
# issue candidacy alone. It never moves a baseline number, so the ratchet still sees the definition.
#
# The seventeen below are issue #356's, written before the scheduled job first runs. They name
# every constructor standing above the argument-count threshold of 10, not only the one that drives
# its file, because `src/17_Optimisation/05_JuMP/03_JuMPOptimiser_a.jl` holds a second at 19. Nineteen
# definitions sit over seventeen keys, because the key holds no line.

[[exemption]]
path = "src/03_InputData/10_ReturnsResult.jl"
definition = "ReturnsResult"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/10_OnlinePortfolioSelection/04_ProgrammeAllocationSet.jl"
definition = "ProgrammeAllocationSet"
metric = "arg"
rationale = "keyword-constructor-arity"

# The split of `03_Preprocessing.jl` into the price-to-returns run. Each of these three definitions
# moved byte-for-byte, and each now drives the maximum of a smaller file rather than sitting under
# the row that grandfathered it. ADR 0074's entry test reads the new file as an addition, so the
# five keys below say what the dead row said.
[[exemption]]
path = "src/03_InputData/10_ReturnsResult.jl"
definition = "ReturnsResult"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/03_InputData/10_ReturnsResult.jl"
definition = "ReturnsResult"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/03_InputData/10_ReturnsResult.jl"
definition = "port_opt_view"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/03_InputData/11_PricesToReturns.jl"
definition = "prices_to_returns"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/03_InputData/11_PricesToReturns.jl"
definition = "prices_to_returns"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/05_Gerber/03_GerberIQCovariance_a.jl"
definition = "FullGerberIQ"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/05_Moments/05_Gerber/03_GerberIQCovariance_a.jl"
definition = "PartialGerberIQ"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/05_Moments/32_CrossSectionalFactorModel/07_ReturnForecasts/06_TargetReturnForecast.jl"
definition = "TargetReturnForecast"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/10_Prior/01_Base_Prior.jl"
definition = "LowOrderPrior"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/10_Prior/05_BlackLitterman/04_FactorBlackLittermanPrior.jl"
definition = "FactorBlackLittermanPrior"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/10_Prior/05_BlackLitterman/05_AugmentedBlackLittermanPrior.jl"
definition = "AugmentedBlackLittermanPrior"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/02_MeucciEntropyPoolingPrior.jl"
definition = "MeucciEntropyPoolingPrior"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_c.jl"
definition = "EntropyPoolingPrior"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/11_UncertaintySets/04_BootstrapUncertaintySets.jl"
definition = "ARCHUncertaintySet"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/04_Hierarchical/01_Base_ClusteringOptimisation.jl"
definition = "HierarchicalOptimiser"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/03_JuMPOptimiser_a.jl"
definition = "JuMPOptimiser"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/03_JuMPOptimiser_a.jl"
definition = "ProcessedJuMPOptimiserAttributes"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_a.jl"
definition = "NearOptimalCentering"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_a.jl"
definition = "NearOptimalSetup"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/06_Meta/02_NestedClustered.jl"
definition = "NestedClustered"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/06_Meta/03_Stacking.jl"
definition = "Stacking"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/17_Optimisation/06_Meta/04_SubsetResampling.jl"
definition = "SubsetResampling"
metric = "arg"
rationale = "keyword-constructor-arity"

# Issue #706's, added after the seventeen above. `CrossSectionalFactorModel` carries twelve
# fields, so its constructor takes twelve arguments under the same house style.

[[exemption]]
path = "src/05_Moments/32_CrossSectionalFactorModel/03_CrossSectionalFactorModel.jl"
definition = "CrossSectionalFactorModel"
metric = "arg"
rationale = "keyword-constructor-arity"

# Issue #725's. `CrossSectionalFactorPrior` carries fifteen fields, so its constructor takes
# fifteen arguments under the same house style. Twelve of them are the reference
# implementation's own public parameters, one per configuration choice the prior offers.

[[exemption]]
path = "src/10_Prior/11_CrossSectionalFactorPrior.jl"
definition = "CrossSectionalFactorPrior"
metric = "arg"
rationale = "keyword-constructor-arity"

[[exemption]]
path = "src/01_Base/03_PrettyShow.jl"
definition = "@define_pretty_show"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/01_Base/03_PrettyShow.jl"
definition = "@define_pretty_show"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/01_Base/05_Preferences.jl"
definition = "apply_preferences!"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/01_Base/05_Preferences.jl"
definition = "apply_preferences!"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

# The letter splits of ADR 0178 cut eight files near the size threshold. Each definition
# below stood inside its old file's row, and now drives the maximum of the part that holds it.

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_a.jl"
definition = "ep_evar_anchor"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_a.jl"
definition = "ep_evar_grid"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_a.jl"
definition = "ep_rlvar_anchor"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_a.jl"
definition = "ep_rlvar_grid"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_b.jl"
definition = "ep_add_evar_view!"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_b.jl"
definition = "ep_add_rlvar_view!"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_c.jl"
definition = "EntropyPoolingPrior"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_c.jl"
definition = "ep_prior"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/03_EntropyPoolingPrior_c.jl"
definition = "ep_prior"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/03_JuMPOptimiser_a.jl"
definition = "JuMPOptimiser"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/03_JuMPOptimiser_a.jl"
definition = "JuMPOptimiser"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/02_CrossValidation/01_Base_CrossValidation_a.jl"
definition = "PredictionReturnsResult"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/02_CrossValidation/01_Base_CrossValidation_a.jl"
definition = "PredictionReturnsResult"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/02_CrossValidation/01_Base_CrossValidation_b.jl"
definition = "fit_and_predict"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/02_CrossValidation/01_Base_CrossValidation_b.jl"
definition = "fold_loop"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/30_RegimeAdjusted/02_RegimeAdjustedExpWeightedCovariance_a.jl"
definition = "regime_adjusted_covariance_pass!"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/30_RegimeAdjusted/02_RegimeAdjustedExpWeightedCovariance_a.jl"
definition = "regime_adjusted_covariance_pass!"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/30_RegimeAdjusted/02_RegimeAdjustedExpWeightedCovariance_a.jl"
definition = "RegimeAdjustedExpWeightedCovariance"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_a.jl"
definition = "NearOptimalCentering"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_a.jl"
definition = "near_optimal_centering_setup"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_a.jl"
definition = "solve_noc!"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_b.jl"
definition = "get_overall_retcode"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/17_Optimisation/05_JuMP/06_NearOptimalCentering_b.jl"
definition = "solve_noc!"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/05_Gerber/03_GerberIQCovariance_a.jl"
definition = "gerber_iq_weight"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/05_Gerber/03_GerberIQCovariance_a.jl"
definition = "gerber_iq_weight"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/05_Moments/05_Gerber/03_GerberIQCovariance_a.jl"
definition = "iq_add_diagonal"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_a.jl"
definition = "UniverseSets"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_a.jl"
definition = "collect_terms!"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_a.jl"
definition = "collect_terms!"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_a.jl"
definition = "eval_numeric_functions"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_b.jl"
definition = "get_linear_constraints"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_b.jl"
definition = "replace_group_by_assets"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_b.jl"
definition = "get_linear_constraints"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_b.jl"
definition = "replace_group_by_assets"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

# The splits by subject of ADR 0179 moved each definition below unchanged into a file of its
# subject, where it now drives the file's maximum.

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/04_EntropyPoolingSolvers.jl"
definition = "entropy_pooling"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/04_EntropyPoolingSolvers.jl"
definition = "ep_var_views!"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/04_EntropyPoolingSolvers.jl"
definition = "entropy_pooling"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/04_EntropyPoolingSolvers.jl"
definition = "ep_var_views!"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/05_EntropyPoolingMomentViews.jl"
definition = "replace_coprior_views"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/05_EntropyPoolingMomentViews.jl"
definition = "replace_coprior_views"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/02_Tools/05_Propagatable.jl"
definition = "propagatable_parse_body"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/02_Tools/06_ForwardProperties.jl"
definition = "@forward_properties"
metric = "cyc"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "src/02_Tools/06_ForwardProperties.jl"
definition = "@forward_properties"
metric = "cog"
rationale = "a-definition-a-file-split-moved-unchanged"

# The split by subject of the Plots extension (ADR 0179) moved each definition below unchanged
# into a file of its subject, where it now drives the file's maximum.

[[exemption]]
path = "ext/PortfolioOptimisersPlotsExt/04_CompositionPlots.jl"
definition = "PortfolioOptimisers.plot_factor_risk_contribution"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "ext/PortfolioOptimisersPlotsExt/04_CompositionPlots.jl"
definition = "PortfolioOptimisers.plot_risk_contribution"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "ext/PortfolioOptimisersPlotsExt/10_FrontierPlots.jl"
definition = "PortfolioOptimisers.plot_efficient_frontier"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

[[exemption]]
path = "ext/PortfolioOptimisersPlotsExt/10_FrontierPlots.jl"
definition = "PortfolioOptimisers.plot_portfolio_dashboard"
metric = "arg"
rationale = "a-definition-a-file-split-moved-unchanged"

# --- coverage exemptions ---------------------------------------------------
#
# ADR 0082. One row per definition that holds an uncovered line no test can reach, keyed
# (path, definition) and carrying the exact count of lines it stands for.
#
#   [[coverage_exemption]]
#   path       = "src/…/…jl"
#   definition = "show"          # or "<toplevel>" for a line inside no named definition
#   misses     = 3
#   rationale  = "a-named-rationale-block-above"
#
# The key holds no line number. A line number churns on every insertion above it and on every
# reformat, and this repository formats the whole tree in place, so a line-keyed row would be wrong
# more often than it was right. A definition name survives both. ADR 0071 keys a Dismissal on
# (file, kind, message) for the same reason.
#
# The count is part of the claim, and `coverage.jl check` holds it to equality in both directions.
# A claim above the truth is a stale row whose lines were covered. A claim below it is the leak the
# file total cannot see: a line covered elsewhere in the file pays for a new uncovered line inside
# the exempted definition, and the ratchet reads a flat number.
#
# A row here never moves a baseline number. Like the Exemptions above it, it binds one thing only:
# whether the file is **terminal**, which is #404's condition for closing a child map on coverage.
# The ratchet still sees every miss.
#
# #408 built the machinery; the thirteen child maps of #404 write the rows as they sweep. A row is
# written when a line cannot be reached, never when reaching it is merely awkward.
#
# A line reached only by its own `jldoctest` is not exempt. Coverage is the test suite's number:
# the `doctest` job of `Docs.yml` carries no instrumentation, and a doctest asserts a rendering
# rather than a result. "The doctest reaches it" is therefore not a rationale, and the sweep writes
# the test. ADR 0082, issue #411.

[[coverage_exemption]]
path = "src/08_Phylogeny/03_Hierarchical.jl"
definition = "optimal_number_clusters"
misses = 1
rationale = "a-cut-into-one-cluster-is-always-valid"

[[coverage_exemption]]
path = "src/09_ConstraintGeneration/02_LinearConstraintGeneration_a.jl"
definition = "LinearConstraint"
misses = 1
rationale = "a-signature-line-counter-the-test-job-cannot-reach"

# Two constants of one shape, one row each. The `const` line of each holds the `map` closure that
# builds the union, and the count of 1 is the synthetic zero that the line draws.
[[coverage_exemption]]
path = "src/05_Moments/20_Base_Regression.jl"
definition = "MinValStepwiseRegressionCriterion"
misses = 1
rationale = "a-closure-inside-a-top-level-const"

[[coverage_exemption]]
path = "src/05_Moments/20_Base_Regression.jl"
definition = "MaxValStepwiseRegressionCriterion"
misses = 1
rationale = "a-closure-inside-a-top-level-const"

# --- performance dismissals ------------------------------------------------
#
# ADR 0175. A row with `code` covers the one Finding whose printed code matches; a row without it
# covers every Finding of the rule in the definition. Issue #1312 wrote the first rows.

[[perf_dismissal]]
file = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/02_EntropyPoolingMeanViews.jl"
rule = "loop_allocation"
definition = "replace_prior_views"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/10_Prior/06_EntropyPooling/01_Base_EntropyPoolingPrior/05_EntropyPoolingMomentViews.jl"
rule = "loop_allocation"
definition = "replace_coprior_views"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/09_ConstraintGeneration/09_CrossSectionalFactorAxis.jl"
rule = "loop_allocation"
definition = "cross_sectional_factor_sets"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/14_NetReturnsDrawdowns.jl"
rule = "loop_allocation"
definition = "rebuild_weight_path"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/17_Optimisation/02_CrossValidation/03_Combinatorial.jl"
rule = "loop_allocation"
definition = "split"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/17_Optimisation/04_Hierarchical/02_HierarchicalRiskParity.jl"
rule = "loop_allocation"
definition = "_optimise"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/17_Optimisation/04_Hierarchical/03_SchurComplementHierarchicalRiskParity.jl"
rule = "loop_allocation"
definition = "schur_complement_weights"
rationale = "an-allocation-the-loop-keeps"

[[perf_dismissal]]
file = "src/10_Prior/11_CrossSectionalFactorPrior.jl"
rule = "slice_copy"
definition = "prior"
code = "vs[end, :]"
rationale = "a-slice-the-result-keeps"

[[perf_dismissal]]
file = "src/01_Base/06_Messages.jl"
rule = "loop_allocation"
definition = "failed_solve_msg"
rationale = "a-failure-message-is-a-cold-path"
