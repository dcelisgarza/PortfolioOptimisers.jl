# Processed covariance

```@docs
ProcessedCovariance
factory(ce::ProcessedCovariance, args...; kwargs...)
cov(ce::ProcessedCovariance, X::MatNum; dims = 1, kwargs...)
cor(ce::ProcessedCovariance, X::MatNum; dims = 1, kwargs...)
```
