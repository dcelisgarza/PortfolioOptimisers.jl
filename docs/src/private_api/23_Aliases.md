```@meta
Description = "Aliases has no private API in PortfolioOptimisers.jl; its names are in the public API."
```

# Aliases: private API

Aliases has no private API in PortfolioOptimisers.jl; see [`Aliases`](@ref) for its public API.
