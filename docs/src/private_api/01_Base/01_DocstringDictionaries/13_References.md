```@meta
Description = "Reference bullets has no private API in PortfolioOptimisers.jl; its names are in the public API."
```

# Reference bullets: private API

This file defines no name. It fills [`ref_dict`](@ref) with the `# References` bullets, one for each key of `docs/src/References.bib`. [`unique_key_dict!`](@ref) refuses a key that another entry holds with a different description.
