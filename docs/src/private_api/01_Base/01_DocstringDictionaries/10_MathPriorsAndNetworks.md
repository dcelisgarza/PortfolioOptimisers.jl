```@meta
Description = "Prior and network notation has no private API in PortfolioOptimisers.jl; its names are in the public API."
```

# Prior and network notation: private API

This file defines no name. It fills [`math_dict`](@ref) with the notation of entropy pooling, phylogeny, centrality, clusters and preselection. [`unique_key_dict!`](@ref) refuses a key that another entry holds with a different description.
