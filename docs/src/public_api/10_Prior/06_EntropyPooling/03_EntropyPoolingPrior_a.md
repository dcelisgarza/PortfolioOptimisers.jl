```@meta
Description = "Entropy Pooling (a) has no public API in PortfolioOptimisers.jl; its names are in the private API."
```

# Entropy Pooling (a)

Every name of this topic is private. The [private page](@ref private-api-entropy-pooling-a) documents them.
