```@meta
Description = "Messages has no public API in PortfolioOptimisers.jl; its names are in the private API."
```

# Messages

Messages has no public API in `PortfolioOptimisers.jl`; its names are in the private API.
