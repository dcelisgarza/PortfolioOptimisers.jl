```@meta
Description = "Preferences has no public API in PortfolioOptimisers.jl; its names are in the private API."
```

# Preferences

Preferences has no public API in `PortfolioOptimisers.jl`; its names are in the private API.
