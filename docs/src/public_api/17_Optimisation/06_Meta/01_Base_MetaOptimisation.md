```@meta
Description = "Meta optimisation has no public API in PortfolioOptimisers.jl; its names are in the private API."
```

# Meta optimisation

Meta optimisation has no public API in PortfolioOptimisers.jl; see [`Meta optimisation: private API`](@ref) for its private API.
