```@meta
Description = "Clique Hierarchy has no public API in PortfolioOptimisers.jl; its names are in the private API."
```

# Clique Hierarchy

Clique Hierarchy has no public API in PortfolioOptimisers.jl; see [`Clique Hierarchy: private API`](@ref) for its private API.
