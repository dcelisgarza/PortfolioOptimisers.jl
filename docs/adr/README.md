# Architecture decision records — index

This page is the map of `docs/adr/`. Each row names one decision in one line, so you can find the
ADR you need without reading the directory.

How to use it:

1. Find the row for the area that you touch.
2. Open that ADR before you change the behaviour it fixes.
3. An ADR whose decision reached `main` is **amended, never rewritten**. Append a
   `## Amendment (YYYY-MM-DD)` section. Such an ADR describes released behaviour, so one that
   describes superseded behaviour is correct history, not a defect. An ADR whose decision has not
   reached `main` is still a draft. Rewrite it in place.
4. Add a row here when you add an ADR. The row is one line: the number, the title, and what the
   decision settles.

The **Am.** column counts the amendments and gives the date of the last one. A high count marks a
decision that moved many times, so read the amendments as well as the body.

## The decisions

| # | Decision | Am. | What it settles |
| ---- | -------- | --- | --------------- |
| 0001 | [Generalise the pure-propagation `factory` methods](0001-generalise-factory-methods.md) | | One generic `factory` recurses into the fields of an eligible struct. Leaf injectors and logic-bearing methods stay explicit. The mechanism is the `@propagatable` macro. |
| 0002 | [`@propagatable` macro design](0002-propagatable-macro-design.md) | 1 · 2026-08-17 | A field joins propagation only if a tag marks it. Runtime type dispatch alone is not enough. |
| 0003 | [Parallelise the test suite with ParallelTestRunner.jl](0003-parallelise-test-suite.md) | 1 · 2026-08-17 | The runner auto-discovers `test/*.jl` and runs each file in an isolated worker module. |
| 0004 | [Give the JuMP model a typed state interface](0004-typed-jump-model-state.md) | | Category A state is reached through a named, checked accessor interface. Category B keeps the `(expr, key)` convention. |
| 0005 | [Namespace nested risk state by key prefix](0005-prefix-namespaced-risk-state.md) | | A nested risk expression is built under a key prefix. Nothing is saved, unregistered, or restored. |
| 0006 | [Classify the expected-risk input shape with a trait](0006-risk-input-kind-trait.md) | | Each measure answers `risk_input_kind` at its definition site. The routing unions are gone. |
| 0007 | [Feed a reduced return series through `r(x::VecNum)`](0007-precomputed-returns-functor-contract.md) | | The single-argument functor is the canonical "expected risk of the net-return series". No sentinel vector. |
| 0008 | [Extract the JuMP model-assembly pipeline into one deep module](0008-jump-model-assembly.md) | 3 · 2026-08-18 | The invariant middle is one mutating builder. The per-optimiser head and tail stay in place. |
| 0009 | [Matrix-processing order is a sequence of step symbols](0009-matrix-processing-order-as-step-symbols.md) | | The `order` field holds a tuple of step symbols. The other fields do not change. |
| 0010 | [Unify the `*_view` family; split the propagation tags](0010-unify-view-family-and-split-propagation-tags.md) | 3 · 2026-08-21 | One `port_opt_view` verb. `@prop` splits into `@fprop` (factory) and `@vprop` (view). |
| 0011 | [One deep optimisation Result](0011-deep-optimisation-result.md) | 1 · 2026-08-16 | The result side mirrors the JuMP estimator structure. The change is additive. |
| 0012 | [Deepen the prior→measure moment binding with `@pprop`/`@cprop`](0012-prior-measure-binding-tags.md) | | A tag names either a mechanism or a source. The family grows from two axes to five. |
| 0013 | [Generate property forwarding with `@forward_properties`](0013-property-forwarding-macro.md) | 1 · 2026-08-24 | A standalone macro beside the struct emits `getproperty` and `propertynames` from explicit rules. |
| 0014 | [Group the user guide and the examples by pipeline stage](0014-group-docs-by-pipeline-stage.md) | | Both narrative halves follow the `CONTEXT.md` pipeline spine, one subdirectory per group. |
| 0015 | [A role suffix is for disambiguation](0015-disambiguation-suffix-naming.md) | | A type takes the bare concept word when that word is unambiguous. A suffix is added only to earn the bare name back. |
| 0016 | [Uncertainty sets own their posdef and moment configuration](0016-uncertainty-sets-own-their-posdef-and-moment-config.md) | | An uncertainty-set estimator carries its own configuration. It never reaches into the prior instance. |
| 0017 | [`FiniteAllocationInput` is an Estimator, not a Result](0017-finite-allocation-input-is-an-estimator.md) | | The allocation input is one struct on the Estimator tree. The deviation from the Result precedent is deliberate. |
| 0018 | [Risk-measure ↔ optimiser compatibility is a type-derived trait](0018-risk-measure-optimiser-compatibility-trait.md) | | `supported_risk_measures(::Type{<:Opt})` is the single primitive. The predicate and the docs table derive from it. |
| 0019 | [Drop the Python dependency](0019-drop-python-dependency-native-statistics.md) | | Histogram binning and the block bootstrap are native Julia. `PythonCall` and `CondaPkg.toml` are removed. |
| 0020 | [`NormError` is a shared Base primitive](0020-normerror-shared-base-primitive.md) | | The norm family is Base vocabulary. The `Lx*Tracking` types are renamed `Lx*Norm`. |
| 0021 | [One MIP constraint family over an `AbstractMIPSpace`](0021-unify-mip-constraints-over-mip-space.md) | | The three points of difference become a small interface. The builders are written once. |
| 0022 | [A data-carrying `SmythBrobyKernel` folds the Gerber markers into one algorithm](0022-smythbroby-kernel-unifies-gerber-markers.md) | 1 · 2026-08-17 | The two axes separate into dispatch. One comovement loop drives the per-run scoring state. |
| 0023 | [Nested optimisers propagate structured failure diagnostics](0023-structured-optimisation-failure-diagnostics.md) | | `OptimisationFailure.res` is a named tuple that names each sub-problem and its return code. |
| 0024 | [One `scalarise` seam](0024-single-scalarise-seam.md) | | One generic `scalarise(f, ::Scalariser, itr; by)`. The per-optimiser copies are deleted. |
| 0025 | [Enumerate the equation parser's callable functions](0025-enumerated-parser-allowlist.md) | 1 · 2026-07-06 | An explicit `Symbol => Function` table is the only source of what the parser can call. No lookup in `Base`. |
| 0026 | [Keep the lenient constraint-name default](0026-lenient-constraint-names-with-suggestions.md) | 1 · 2026-07-06 | `strict = false` stays the default. The diagnostic improves instead. |
| 0027 | [Cap the equation parser's length and depth](0027-cap-equation-parser-recursion.md) | 1 · 2026-07-06 | Two static, overridable caps at the trust boundary. The parser fails closed with a typed error. |
| 0028 | [`Pipeline` is a root workflow Estimator](0028-pipeline-workflow-estimator.md) | 5 | A linear step list over an accumulating context. The whole workflow becomes the unit that is fitted per fold. |
| 0029 | [Asset selection is returns preprocessing](0029-asset-selection-is-returns-preprocessing.md) | | A selector is a preprocessing subfamily. The fitted universe is state on one shared result. |
| 0030 | [Time-dependent inputs are in-field wrappers](0030-time-dependent-constraints.md) | 1 · 2026-08-19 | `TimeDependent(val; default)` is consumed by whichever fold loop processes it. |
| 0031 | [The holdout split is a pipeline step](0031-holdout-split-as-a-pipeline-step.md) | | `TrainTestSplit` is pinned to the first position, and it excludes cross-validation. |
| 0032 | [Quintile portfolios are an uncertainty set](0032-quintile-portfolios-are-an-uncertainty-set.md) | 1 · 2026-08-16 | There is deliberately no `QuintilePortfolio` type. The recipe is an uncertainty-set shape. |
| 0033 | [Split the MIP file into an indicator layer and topical emitters](0033-split-mip-file-into-indicator-layer-and-emitters.md) | | The MIP file keeps only the indicator layer. Each consumer moves to a file named for its feature. |
| 0034 | [The MIP indicator bundle lives in Model State](0034-mip-indicator-bundle-lives-in-model-state.md) | | The bundle is a first-class state entry, read back through the typed accessors. |
| 0035 | [The `ucs` extraction shares post-sampling kernels](0035-ucs-extraction-is-post-sampling-not-halves.md) | | Prior, seeding, and sampling stay in each public body. Only the pure post-sampling construction is shared. |
| 0036 | [A custom objective term is a penalty contribution](0036-custom-objective-terms-are-penalty-contributions.md) | 1 · 2026-08-18 | The hook adds to the penalty accumulator. It never touches the objective expression. |
| 0037 | [Model State is reached only through typed accessors](0037-model-state-accessor-interface.md) | 2 · 2026-08-17 | One memoise combinator, not per-key accessors. The seam lock fails closed. |
| 0038 | [Optimisers own pipeline routing](0038-optimisers-own-pipeline-routing.md) | 2 · 2026-08-18 | The optimiser decides which field a target lands in. The `Pipeline` owns only the fan-out. |
| 0039 | [A windowed estimator is generated from one declaration](0039-windowed-estimators-are-generated-from-one-declaration.md) | | `@windowed_estimator` emits a whole family member from one block. The types do not merge. |
| 0040 | [The capability catalogue curates grouping, not descriptions](0040-capability-catalogue-curates-grouping-not-descriptions.md) | 1 · 2026-08-18 | The page is generated. The declaration curates the tree only. |
| 0041 | [One `RESOURCE_LIMITS` cap per sink](0041-one-resource-cap-per-sink.md) | 3 · 2026-08-17 | Every sizing sink gets its own field. A cap is never reused, and it is named after the field it guards. |
| 0042 | [`Impute` is a weak dependency](0042-impute-is-a-weak-dependency.md) | 1 · 2026-09-03 | One seam, `apply_impute_method`, which the extension extends. |
| 0043 | [A `nothing` observation weight means unweighted](0043-nothing-observation-weights-means-unweighted-not-unavailable.md) | | `nothing` means that no weights were asked for. A weight that cannot be resolved throws. |
| 0044 | [A matrix source is named, not flagged](0044-matrix-sources-are-named-not-flagged.md) | | `cle_pr::Bool` becomes `x_src::Symbol`. A clean break, no deprecation path. |
| 0045 | [A feature matrix is data, not estimator configuration](0045-a-feature-matrix-is-data-not-estimator-configuration.md) | 10 · 2026-09-05 | The panel rides on the result beside the returns, and the matrix is stacked from it at the point of use by the Panel Fields a selector names. A taxonomy enters the panel as a categorical Panel Field, and the graded program is deleted. The estimator holds only configuration. |
| 0046 | [Wrapping priors forward by default](0046-wrapping-priors-forward-by-default-and-document-every-drop.md) | 12 · 2026-08-18 | Forward when forwarding is correct. Drop only where forwarding states something false, and document every drop. |
| 0047 | [A constraint is re-based only if it is linear in `w`](0047-a-constraint-is-re-based-only-if-it-is-linear-in-w.md) | 4 · 2026-08-16 | A wrapper type declares the re-basis, so a constraint that is not a linear form cannot state one. |
| 0048 | [A network relates by its separation](0048-a-network-relates-by-its-separation-and-weights-by-what-selected-it.md) | 6 · 2026-08-17 | Truncation and decay are two knobs, and only one of them cuts. A weight comes from what selected the edge. |
| 0049 | [A similarity reaches the PMFG only if it cannot go negative](0049-a-similarity-reaches-the-pmfg-only-if-it-cannot-go-negative.md) | | The non-negativity is a type bound, not a runtime check. A zero weight is an absent edge, so the graph needs a strictly positive one. |
| 0050 | [An uncertainty set carries the quantity it bounds](0050-an-uncertainty-set-carries-the-quantity-it-bounds.md) | | Each result gains one optional field for the quantity it is a neighbourhood of, and that quantity wins. |
| 0051 | [A prior-derived slot may hold the method instead of the value](0051-a-prior-derived-slot-may-hold-the-method-instead-of-the-value.md) | 4 · 2026-08-28 | The noun is a Deferred Quantity. Four quantities defer: `mu`, `sigma`, `kt`, `sk`. |
| 0052 | [A return expression is a weighted sum of terms](0052-a-return-expression-is-a-weighted-sum-of-terms.md) | 1 · 2026-08-16 | The plural noun is the term, not the characteristic. |
| 0053 | [`scale` is a combination weight](0053-scale-is-a-combination-weight.md) | 2 · 2026-08-18 | The weight an element carries inside a combination. One element is not a combination, so the weight is inert. |
| 0054 | [A degeneracy guard tests the expression](0054-a-degeneracy-guard-tests-the-expression-not-the-term-type.md) | | The expression is degenerate when it is identically zero, by whichever route it got there. |
| 0055 | [Recursive bisection owns its branch order](0055-recursive-bisection-owns-its-branch-order.md) | | An optimiser declares `branchorder` only if the leaf permutation is optional for its allocation. |
| 0056 | [The Julia setup action floats on `@latest`](0056-the-julia-setup-action-floats-on-latest.md) | | Every `setup-julia` step uses `@latest`. A setup action is a toolchain installer. |
| 0057 | [A range risk measure is its base measure applied twice](0057-a-range-risk-measure-is-its-base-measure-applied-twice.md) | | `range_tails(r)` returns the two point measures. Each tail builds through the single-tail seam. |
| 0058 | [The `dims` guard and the orientation are one call](0058-the-dims-guard-and-the-orientation-are-one-call.md) | 1 · 2026-09-01 | `dims_oriented(dims, A)` validates and orients, so a caller cannot orient without validating. |
| 0059 | [A drawdown tail measure is its returns twin on another series](0059-a-drawdown-tail-measure-is-its-returns-twin-on-another-series.md) | | `risk_series` is the one place the substitution is made. Each builder is written once. |
| 0060 | [A constructor docstring block is a copy, and a census gates it](0060-a-constructor-docstring-block-is-a-copy-and-a-census-gates-it.md) | | The block stays hand-written. A test parses the source text and makes the drift loud. |
| 0061 | [The propagation tag set is data](0061-the-propagation-tag-set-is-data.md) | 1 · 2026-08-21 | `PROP_TAG_NAMES` is the table. The recognition layer derives from it, and the field transform is keyed on the channel as well as the tag. |
| 0062 | [The frontier sweep is one seam](0062-the-frontier-sweep-is-one-seam.md) | | The sweep lives in the Model State interface, beside the two frontier readers. |
| 0063 | [The risk-free rate is added once per prior](0063-the-risk-free-rate-is-added-once-per-prior.md) | 1 · 2026-08-27 | One site, `apply_rf`. A level the prior mean lacks goes on before the blend, never after it. |
| 0064 | [The entropy pooling prior dispatches on its algorithm](0064-the-entropy-pooling-prior-dispatches-on-its-algorithm.md) | | `prior` has one method on the bare estimator, which forwards the algorithm as a value. |
| 0065 | [The weighted moment seam densifies its observations](0065-the-weighted-moment-seam-densifies-its-observations.md) | | The weighted methods materialise the observation matrix before the call. |
| 0066 | [A sub-portfolio enumeration is an adapter](0066-the-sub-portfolio-enumeration-is-an-adapter.md) | | `SubPortfolioUniverse` is the adapter, and the module is written once. |
| 0067 | [The cross-validation fold loop is one seam](0067-the-cross-validation-fold-loop-is-one-seam.md) | 3 · 2026-09-04 | One `fold_loop`, beside `parallel_folds` and `run_folds`. |
| 0068 | [A Black-Litterman caller states its axis, not its key](0068-a-black-litterman-caller-states-its-axis-not-its-key.md) | | The last argument of `bl_preroll` names the axis. The callee resolves the key. |
| 0069 | [A tail view carries its own level and formulation](0069-a-tail-view-carries-its-own-level-and-formulation.md) | | The group holds `alpha` and `alg`. `nothing` picks the cheapest exact formulation. |
| 0070 | [An ambiguity set is a reading, and what is missing is a calibrated radius](0070-an-ambiguity-set-is-a-reading-and-a-calibrated-radius.md) | 3 · 2026-08-29 | No set family. A radius slot admits the rule that computes it, resolved by Factory. |
| 0071 | [A dismissed JET report is keyed by file, kind and message](0071-a-dismissed-jet-report-is-keyed-by-file-kind-and-message.md) | 1 · 2026-08-22 | A Report Fingerprint is the attribution file, the report kind and the message, plus the builtin on a `BuiltinErrorReport`. No line, no stack trace, no signature render. A Dismissal covers the class and cites a shared Rationale, and so does an Exemption; re-affirmation on a Julia bump binds the JET side alone. |
| 0072 | [The complexity gate measures `src/` and `ext/`, and a Declaration Macro is measured at its declaration](0072-the-complexity-gate-measures-src-and-ext.md) | | Scope is `src/` and `ext/`; every other tracked file is a named Unmeasured Path. A Declaration Macro is measured where it is declared, and the Expansion Bound holds what it emits. |
| 0073 | [The code-health baseline is four TOML files, split on who writes them](0073-the-code-health-baseline-is-four-toml-files.md) | | Four TOML files under `code_health/`, one inline table per line. Every file in scope gets a row, JET is keyed by run and attributed to its deepest repository frame, and a refresh lowers by default. Each threshold sits at a cliff in this repository's distribution, argument count at 10, with 17 constructor Exemptions written up front. |
| 0074 | [The baseline's row set is total, and a rename pairs by equal measurement](0074-the-baseline-row-set-is-total-and-a-rename-pairs-by-measurement.md) | | The gate compares two file sets and never invents a number. A rename pairs a dead row with a new path by equal measurement; JET pairs on the raw count. An added file must pass the scheduled job's candidacy test. |
| 0075 | [A run that trips publishes the Refresh Artifact that clears it](0075-a-run-that-trips-publishes-the-refresh-artifact.md) | | A fork pull request measures, and no workflow tests for a fork. A failing run uploads the drop-in baseline the refresh wrote; a provenance mismatch uploads none. The gate measures the merge commit, and one message gives three ordered routes. |
| 0076 | [The code-health pass rule is a ratchet against a committed baseline](0076-the-code-health-pass-rule-is-a-ratchet.md) | | A file passes when its number has not risen above the recorded one. The gate is green the day it lands, a rise needs an explicit `--accept-rise`, and the thresholds drive candidacy for the scheduled job and never the gate. |
| 0077 | [The code-health gate is two workflows, pinned by one committed Manifest](0077-the-code-health-gate-is-two-workflows-pinned-by-a-manifest.md) | | `Complexity.yml` takes no `paths:` filter and `JET.yml` takes one. `code_health/Manifest.toml` is the single source of every version, its `julia_version` is the Julia pin, and one entry script per generated file takes `check`, `refresh` or `refresh --accept-rise`. |
| 0078 | [The scheduled job files a file once, and refiles it when the number rises](0078-the-scheduled-job-files-a-file-once-and-refiles-it-on-a-rise.md) | | The offender is a file, filed at most once under a five-open cap and ranked JET first. It refiles when the baseline stands above the row at the previous issue's `closed_at`, and an Exemption cites an approved Rationale where a new Rationale needs the maintainer. |
| 0079 | [A group pair view is one row per spanned pair, and `prior(gA, gB)` resolves per pair](0079-a-group-pair-view-is-one-row-per-spanned-pair.md) | | A view over a pair of groups emits one constraint row per spanned asset pair. `prior(gA, gB)` resolves to one value per pair, the group methods carry the `:rho` and `:cov` tags, and a `RhoParsingResult` may hold a vector right-hand side. |
| 0080 | [An undefined standard deviation makes `StandardisedValue` the mean](0080-an-undefined-standard-deviation-divides-by-one.md) | | A corrected standard deviation needs two values, so a one-value vector divides by one and reduces to its own mean. A two-asset cluster then contributes a finite dispersion, and `SecondOrderDifference` keeps `StandardisedValue()` as its default. |
| 0081 | [The docstring standard states the model a docstring builds](0081-the-docstring-standard-states-the-model-it-builds.md) | 3 · 2026-08-24 | `# JuMP formulation` names every entry a body registers — a variable, an expression, a row and the objective, one subsection each — and `## Relaxation` states the bound when the encoding is not exact. `# Algorithm` holds a procedure as numbered steps, and a selector tag is never forced to carry a section. A field description interpolates when more than one field uses it. The Scope widens to `ext/`. The gate reads the manifest's `swept` flag: `# JuMP formulation` by a per-unit presence rule triggered on any JuMP model-building macro, with a check per subsection that macro demands, and `# Algorithm` by a per-file floor the row records. |
| 0082 | [The coverage terminal condition is a per-file ratchet and an exemption named by definition](0082-the-coverage-terminal-condition-is-a-per-file-ratchet-and-a-named-exemption.md) | 2 · 2026-08-27 | The ratchet is per file and the miss count binds, not the percentage. A file added to the tree enters terminal. A Coverage Exemption is keyed (path, definition), states its exact count, is held to equality in both directions, and binds the terminal condition rather than the baseline. Nothing in the coverage provenance binds, and every `codecov.yml` status is informational. A line reached only by its own `jldoctest` is a miss, and no exemption may cite an example. |
| 0083 | [The markdown lint gate reads hand-written files, and a duplicate heading is judged among siblings](0083-the-markdown-lint-gate-reads-hand-written-files.md) | | `.markdownlintignore` holds the scope: the four generated documentation paths and `research/` are out of it, and every other tracked `.md` file is in it. `MD024` is scoped to `siblings_only`, so an ADR amendment may restate `Decision` and `Verification` under its own parent. Seven ADR code fences take a language and three ADR tables are padded. |
| 0084 | [The sweep job reopens a closed child map, and files the addition as its sub-issue](0084-the-sweep-job-reopens-a-closed-child-map-and-files-the-addition.md) | 1 · 2026-08-24 | The trigger is one condition, `swept = false` under a CLOSED child map, and that trigger is also the deduplication. The job reopens the map and #404, opens one `sweep` sub-issue per file, and takes no cap. It keeps `contents: read` because the census keeps a pull request red until the row arrives by hand, and it reads a child map's issue number from the tracker rather than from a committed table. |
| 0085 | [The docstring standard is rules and pointers, and `# Details` is abolished](0085-the-docstring-standard-is-rules-and-pointers.md) | | The Authority carries rules and pointers: its 380-line fictional `## Complete Example` becomes a table of reference docstrings that name gated units. `# Details` is abolished over 299 docstrings, with four destinations chosen by the subject of the fact, gated by zero sections in a swept file and by a library total that may not rise. A shared symbol becomes a `math_dict` key and siblings share the equation form. A prompt holds no template, a sweep ticket carries a `## Routing` block, and `test_46` gates a dead citation and a stale count. |
| 0086 | [An alias docstring links its canonical unit and restates nothing](0086-an-alias-docstring-links-its-canonical-unit-and-restates-nothing.md) | | An alias links its canonical unit and restates nothing. Three kinds carry three section sets: an acronym alias none, a factory alias `# Validation` alone and only when its body raises, and a dispatch alias `# Related` plus `# References`. A dispatch alias is a `const` bound to a type expression, so a union, a container and a parametrised form are one kind. No alias carries `# Fields`, `# Constructors`, `# Arguments`, `# Returns` or `# Examples`, and none carries `# Algorithm`, `# Mathematical definition` or `# JuMP formulation`. The gate forbids a section library-wide, demands `# Related` in a swept file, and ratchets the rest. |
| 0087 | [The library writes one bias-correction default, and does not track the upstream one](0087-one-written-bias-correction-default-not-the-upstream-one.md) | | Upstream has no single default to track: `Statistics.var` and `Statistics.cov` correct, `StatsBase.SimpleCovariance` does not, and `StatsBase`'s weighted reductions resolve `nothing` to `false` while announcing a flip to `true`. A library that writes no `corrected` answers `2.91667` for a variance and `2.1875` for a covariance over the same data, so the sentinel was implemented, measured and rejected. Every site writes `corrected = true`, including the `StatsBase.SimpleCovariance` inside `GeneralCovariance`, which is load-bearing rather than redundant. No constructor guards the plain `Weights` combination, and the raise ships documented on every path. |
| 0088 | [A moment estimator's observation weights weight its centre, not its deviations alone](0088-a-moment-estimators-weights-weight-its-centre.md) | | `SimpleVariance` answered one data set with two numbers: a vector centred on the weighted mean, a matrix on the unweighted one. The matrix centre now resolves through `factory(me, ve.w)`, so `w` weights the centre as well as the deviations and the estimator's own `w` wins over the weights of `me`. `Coskewness` and `Cokurtosis` take the same expression. `Covariance` held no weights to read, so it gains a `@wprop w` beside `me`, `ce` and `alg`, and `covariance_centre_and_estimator` sends `w` into both — option 2 of #492, which makes the four one shape. `factory` already wrote `w` at every level, so no internal call site moves; only a hand-built weighted estimator does. Weights inside `ce` alone still centre unweighted, and the `mean` keyword stays the escape hatch. |
| 0089 | [A histogram edge is widened by the spacing at the value, not by the epsilon of the type](0089-a-histogram-edge-is-widened-by-the-spacing-at-the-value.md) | | `calc_hist_data` widened both edges by `eps(eltype(x))`, the epsilon of the type rather than the spacing at the value. At a magnitude of two or more that is half a unit in the last place, so the upper edge rounded back onto the maximum and the largest observation was binned out, and a constant column gave `NaN`. The lower edge is now the minimum itself, because a bin is closed on the left, and the upper edge is `nextfloat` of the maximum. No pinned number of the library moves: the test data is returns, where the shipped widening already worked. |
| 0090 | [A zero Gerber band edge makes an exactly zero return neutral](0090-a-zero-gerber-band-edge-makes-a-zero-return-neutral.md) | | `GerberCovariance` accepts `t = 0`, where both closed band comparisons hold on an exactly zero return, so the observation was marked up and down at once and the diagonal was not one. `gerber_updown` now adds a sign test to each band, which is redundant for a positive band edge and binds only at a zero one. `t = 0` stays legal and becomes the sign concordance: a zero return is neutral, not both. `GerberIQCovariance` carried the same edge in the shape of its own gate, which at `c = 0` dropped nothing and let a zero return join the neutral count, so the `Gerber1` diagonal was not one and the reduction to `GerberCovariance` failed there; `iq_crossed` states the rule once for that family. `SmythBrobyCovariance` carried it a third time on its indecision zone, where `c2 = 0` inflated the neutral count and the `*1` diagonal was not one, and `sb_crossed` states the rule for that family. Its confusion zone takes no sign test: it reads the **raw, uncentred** return, whose zero is an arbitrary point of the scale of the data, and it only rejects and never classifies. No result at a positive threshold moves. |
| 0091 | [The Smyth-Broby severity exponent is non-negative, and an infinite one is legal](0091-the-smyth-broby-severity-exponent-is-non-negative.md) | | `SmythBrobyCovariance` guarded three of its four numeric fields, and a negative `n` inverted the divergence penalty of `sb_delta`, so the diagonal was zero and the result was not a correlation matrix. `n` is now guarded by `assert_nonneg`, which rejects a negative `n` and `NaN` and keeps `n = 0` and `n = Inf`, because each of those two names a statistic that runs. The three thresholds keep the finite guard: they are read on the scale of the data, where `Inf` admits no observation, while an infinite exponent is a hard divergence gate. The source's `0 < c_1 <= 1` is not enforced, for the same reason. |
| 0092 | [A degenerate shrinkage sample raises, rather than returning `NaN` or `Inf`](0092-a-degenerate-shrinkage-sample-raises-rather-than-returning-nan.md) | | Three legal inputs made `ShrunkExpectedReturns` answer `NaN` or `Inf`: a one-asset sample under `JamesStein` with `GrandMean` or `VolatilityWeighted`, a one-asset sample under `BodnarOkhrinParolya` with any target, and a square or wide returns matrix under `BodnarOkhrinParolya`. Each is a property of the published closed form, so each now raises a `DomainError` at its own site: a zero `dot(mu - b, mu - b)`, a zero Cauchy-Schwarz gap `u * v - w^2`, and the estimator's own published condition `T > N`. Every test is exact, so no non-degenerate result moves. `BayesStein` has a strictly positive denominator and gains no guard. |
| 0093 | [A co-movement matrix carries a unit diagonal](0093-a-co-movement-matrix-carries-a-unit-diagonal.md) | | An asset that crosses no threshold gives `comovement_ratio` a zero denominator for every pair it belongs to, and the guarded zero reached the diagonal, where a correlation is one by definition. `posdef!` divided by the root of that zero and raised `ArgumentError: matrix contains Infs or NaNs`, naming neither the asset nor the cause. The new `comovement_unit_diagonal!` writes one onto a **zero** diagonal entry after the reduction and before the repair, at all five sites of the Gerber, Gerber IQ and Smyth-Broby families. The `iszero` guard is load-bearing: `posdef!` reads its diagonal with an exact `isone` test, and writing an exact one over a `Gerber2` diagonal that is already one to within a unit in the last place flips that test, drops the `cov2cor!` clamp, and moves a pinned covariance by `0.0355` on a sample with no degenerate asset. The only entry that moves is the diagonal entry of a quiet asset, from `0` to `1`. The zero row stays: the asset has no measured co-movement with anyone. ADR 0094 later made the Gerber IQ `Gerber2` diagonal exactly one, so the guard now carries the classic Gerber and Smyth-Broby families alone. |
| 0094 | [The `Gerber2` bound is a template condition, and its denominator is read per pair](0094-the-gerber2-bound-is-a-template-condition-read-per-pair.md) | | `GerberIQCovariance` under `Gerber2` answered entries outside `[-1, 1]` from two causes. The template: the source's internet appendix proves `n_ab^2 <= n_aa n_bb` over every channel that joins two magnitude classes **necessary and sufficient** for the bound, and `clamp_gerber_iq_n` enforced four of `FullGerberIQ`'s fifteen and two of `PartialGerberIQ`'s six. Four shipped `FullGerberIQ` defaults broke the rest, each written from two **mixed** weights rather than the two **diagonal** ones, and a four-row sample returned `1.0299`. The scaler: an asset's class moves with its partner under a pair-dependent `sc`, so the assembled diagonal recorded a different class from the numerator, and a `min` scaler returned `1.224` on `SP500`. The clamp now covers every such channel, with a case split for `PartialGerberIQ`'s discordant boundaries; every default is the geometric mean of its two diagonal weights, so the clamp moves no default; and `Gerber2` accumulates `di` and `dj` per pair through the new `iq_add_diagonal`, so `gerber_IQ` no longer calls `standardise_comovement!`. The diagonal is then exactly one, and `posdef!` keeps its exact `isone` test. Two golden columns move, and the classic reduction stays bit-exact for `Gerber0` and `Gerber1` and exact to one `eps` for `Gerber2`. |
| 0095 | [The calibration channel is parallel to the Deferred Quantity, and the rule is in the bound](0095-the-calibration-channel-is-parallel-and-the-rule-is-in-the-bound.md) | | A slot admits the rule that computes its number. `resolve_calibration_slot`, `calibration_slots`, `resolve_calibration_slots` and two `assert_` verbs sit beside the five verbs of ADR 0051, and a rule stays out of the `DeferredQuantity` union: a rule fits nothing, and it reads the observation weights and the solver that `resolve_slot` does not carry. The entry point is shared, the deferred step runs first and the calibration step runs on its result, so a container may name one key in both declarations. The resolution is derived from the declaration, and two readings put a type outside the derivation: an order between its slots, which the Calibration Context states, and a slot key that is not the field's name, which the three regularisation keys are. A `PROP_TAG_CHANNELS` row was refused because a generated method rewrites a field from a source, and it would emit one method per struct where the derivation is one method for the library. Five rule families, each slot bound to one of them and to a plain `Function`, so a rule of another family is refused at construction and no guard method is written. The slot names the end of the distribution, so one bound serves both ends and a Range head slot defaults from its tail slot. `is_calibration_rule` is the marker rather than the `Function` type, because a container also declares children and one of them is a function that computes no quantity. `LpRegularisation.val` is the one bound that names two quantities and the one slot that admits no function, and two `assert_` methods part the two readings. The joint `@argcheck` stays the whole ordering validation, and nothing reorders a caller's pair. Eleven rules ship, a rule is anything callable, and it gets the solver on both routes. A slot key names no quantity, so the owner states its series marker in the `CalibrationContext` it resolves against, and a schedule stops at the host. |
| 0096 | [The prior `factory` resolves Deferred Quantities last, after every selection](0096-the-prior-factory-resolves-deferred-quantities-last.md) | | The generated prior `factory` used to resolve first and select off the resolved struct, and no ADR wrote that order down. It now selects every `@pprop` and `@cprop` field off `x` and hands the selected struct to `resolve_deferred_quantities`, so a deferred slot and a Calibration Rule see the solver, the observation weights and the children already settled, and a rule may call `ERM` or `RRM`. `sel` gains a Deferred-Quantity arm and a Calibration-Role arm, because a slot the caller filled with the method that computes the value is a stated slot; `ThirdCentralMoment.mu` is the one field that is both `@pprop` and deferrable. The resolver carries the solver as a third argument, and the `JuMP` route threads it because no selection runs there. |
| 0097 | [A norm ceiling is not an ambiguity radius, and it takes its own rule family](0097-a-norm-ceiling-is-not-a-radius-and-takes-its-own-family.md) | | A radius is the coefficient of a norm penalty in the objective, and a ceiling bounds that norm in a constraint, so the two are different quantities. `l2c`, `linfc` and `lpc` take a rule under a family and a `Num_NormCeilCal` bound of their own, and ADR 0095's mechanism is otherwise unchanged. `EffectiveAssetFloor` is the one rule that ships: it states a **fraction of the universe** and reads the asset count off the Prior, so the floor re-derives per fold, per cluster and per subset view where a pasted number holds still. The count it inverts is the order-`p` effective number of assets, which is `number_effective_assets` taken to an arbitrary order. The reciprocal `p`-norm reading that the three constraint builders documented equals that count at `p = 2` alone, and it reports one hundred for ten equally weighted assets at `p = 3`. The norm order belongs to the constraint rather than to the rule, so the constraint site states it in the `CalibrationContext` and no rule carries an order of its own. `LpRegularisation.val` is the one dual-use slot in the library, so `Num_AmbRadNormCeilCal` admits both rule families and no plain function, and `assert_penalty_coefficient_role` and `assert_norm_ceiling_role` part the two readings in `JuMPOptimiser`'s own constructor. |
| 0098 | [An estimator value algorithm is a value, and the branch a slot admits is in its bound](0098-an-estimator-value-algorithm-is-a-value-and-the-branch-is-in-the-bound.md) | | `EqnType` carried `AbstractEstimatorValueAlgorithm`, so `LinearConstraintEstimator` accepted an algorithm that no consumer of `linear_constraints` could use. The one method written for the case declared `::Option{<:LinearConstraint}` and returned the `Num_VecNum` that `estimator_to_val` computes, so it raised on every call it could receive, and it was uncovered. The family now splits by the shape of the value it returns: `VectorAbstractEstimatorValueAlgorithm` is the branch that returns a `Num_VecNum`, and `UniformValues` subtypes it. `EstValType` takes the branch as a type parameter, so the nine value slots state in their own type which algorithms they resolve. `EqnType` holds equation text and nothing else, the broken method is deleted, and the estimator raises at the `val` keyword the caller wrote rather than three frames later inside `processed_jump_optimiser_attributes`. No documented route moves: every documented user of `UniformValues` is a `WeightBoundsEstimator` slot. |
| 0099 | [Choice Surface membership means the verbs exist](0099-choice-surface-membership-means-the-verbs-exist.md) | | Two exported leaves satisfied the covariance surface's type and not its behaviour: `RegimeAdjustedExpWeightedCovariance` declares no verb at all, and `RegimeAdjustedExpWeightedVariance` declares `var` alone while it sits on that surface. The surface's own `cov` fallback reads `cor`, a leaf with no `cor` reaches `StatsBase`'s generic `cor`, and that reads `cov` straight back, so all four pairings overflow the stack rather than raising a `MethodError`. `test_08l_moment_verb_census.jl` now walks `subtypes` from the three moment surfaces and asks each concrete leaf for its family's verbs, where an answer is a method the library declares BELOW the surface. `cor` must be owned; `cov` may be the Correlation Rescale, whose two unstated preconditions -- an owned `cor` and a `ve` field -- the census holds for the four leaves that take it. The rule names no leaf, so a leaf added in future is covered the day it is written, and the two broken ones are its one named exemption, carried by issue #637 and asserted to still fail so it cannot outlive the repair. `moment_family_setup.jl` holds the split and the predicate once, and the hand-written skip in `test_08d_dims_guard.jl` is deleted at an unchanged count of 41 pairings. |
| 0100 | [Split the base file into one file per concept](0100-split-the-base-file-into-one-file-per-concept.md) | | `src/01_Base.jl` held about eight concepts in 5495 lines under no section marker, and one row in every gate for all of them. It becomes `src/01_Base/`, thirteen files in load order, one per concept: the docstring dictionaries, the type roots, the pretty-show macro, the `ScopedConfig` holders, the load-time preferences, the message builders, the error hierarchy, the type aliases, the observation weights, the `assert_*` family, `VecScalar`, the `NormError` family and the Kaniadakis logarithm. The move is structural: the text is byte-for-byte the text that stood in the base file, and the 139 units, the 40 `# Algorithm` sections and the 423 relevant lines of the old rows are conserved across the new ones. A new base symbol goes in the file named for its concept, and a concept that no file names gets a new file. |
| 0101 | [The size gate counts code lines, and binds only over a threshold](0101-the-size-gate-counts-code-lines-and-binds-over-a-threshold.md) | | No rule about file size was written anywhere, and the 1000-total-line rule a review applied reads false on a tree that is 64.8 % docstring: it flags 40 files, 25 of which carry under 500 code lines. `code_health/size.jl` counts code lines per file instead, classifying each line from `JuliaSyntax.tokenize` comment bytes and `SyntaxNode` docstring bytes, so a field docstring counts as prose and a long string literal inside a function does not count as one. A file's ceiling is `max(threshold, recorded code)`: under 500 code lines a file is free, over it a file may fall and may not rise, and crossing trips. A plain ratchet was rejected as the sum-shaped noise of issue #336, so the threshold is in the pass rule and lives in a `[size]` section of `rulings.toml` rather than in `[thresholds]`. 15 files enter held, and the check is a third step of `Complexity.yml`. |
| 0102 | [The Asset Panel owns its Panel Fields, and the Feature Matrix is derived from them](0102-the-asset-panel-owns-its-panel-fields-and-the-feature-matrix-is-derived-from-them.md) | | Two consumers read per-asset data that is not a return series: map #643's descriptors read one field at a time, and the distance stack reads many at once. A dense `Z` with an integer index is two things that must agree, and every check on the first shape existed to keep them agreeing, so a Panel Field now **owns** its values and its observed mask: `NumericPanelField`, `CategoricalPanelField` (integer codes over levels) and `TensorPanelField`. `AssetPanel` is a vector of fields and the two universe masks; it has one type and two shapes, static with `nothing` masks and time-varying with both. `ReturnsResult` and `PricesResult` hold one field, `pnl`, and `nz`, `Z` and `LowOrderPrior.Z` go. The Feature Matrix is **derived**: one verb stacks selected fields into the matrix a distance measures, and nothing stores it. A blank never reaches a carrier, the builder is a function, `BackwardPanelFill` is documented rather than refused, and the subset invariant is checked rather than coerced. |
| 0103 | [Both regression roots are umbrellas, and a result root states what a member carries](0103-both-regression-roots-are-umbrellas-and-a-result-root-states-what-a-member-carries.md) | | A cross-sectional factor prior fits one regression per observation across the assets, which is the transpose of the operation `AbstractRegressionEstimator` names. Both roots become umbrellas. An estimator child states the geometry it fits, `AbstractTimeSeriesRegressionEstimator` or `AbstractCrossSectionalRegressionEstimator`; a result child states what a member carries, `AbstractLoadingsRegressionResult` or `AbstractCrossSectionalRegressionResult`, because a member can be fitted across the assets and still carry loadings. `StepwiseRegression`, `DimensionReductionRegression` and `Regression` re-parent, and `RegE_Reg` pairs the loadings result with the time-series estimator, because every one of the 33 sites that bind the roots outside their own file reads the loadings matrix. `CrossSectionalRegression(f, eps, n, b)` is a sibling result rather than a widening of `Regression`, because one asset index would slice `M` by row and `eps` by column. The verb is `cross_sectional_regression(cre, Z, X, W)` and not a fourth arity of `regression`, whose result method is a greedy passthrough; `W` is an argument because a two-pass weighting scheme calls the estimator twice on one design. Two members ship, a closed form on the weighted design and a wrapper around an external target, and a four-member `alg` family answers rank deficiency. The build measured that Julia's `\` returns the **minimum-norm** solution for a rank-deficient non-square design and not the basic one the ticket recorded, so the four members split on a **square** singular design instead. |
| 0104 | [The input-data files move into a directory of their own](0104-the-input-data-files-move-into-a-directory-of-their-own.md) | | The top level of `src/` held four files under one `03_` prefix, the only repeated prefix there, and the prefix said nothing about their load order. They are one subject -- the input data an optimisation reads before any moment is estimated -- and every other multi-file subject in `src/` already has a directory. They become `src/03_InputData/`, four files numbered in load order: `01_AssetPanel.jl`, `02_AssetPanelBuilder.jl`, `03_Preprocessing.jl`, `04_SyntheticAssetPanel.jl`. The move is structural: no line of the four changes, and every gate number each row carried is carried by the same row under the new path, which is what a refresh would write because all four gates call `CodeHealth.pair_renames`. The API pages do not move, by ADR 0100's rule that a page is checked by the units it renders. An ADR that names an old path in prose keeps it, by the same rule. `synthetic_asset_panel` stays in `src/`: it is exported public API and an example cannot reach a fixture under `test/`. |
| 0105 | [A `nothing` field is hidden at the REPL, and shown in the docs](0105-a-nothing-field-is-hidden-at-the-repl-and-shown-in-the-docs.md) | | `@define_pretty_show` printed every field, so a field that held `nothing` cost a doctest edit at every site that rendered its owner and bought the reader nothing: one new field on the eight moment estimators of #308 moved 237 sites. The shipped default of `SHOW_NOTHING_FIELDS` hides such a field, and the doctest drivers, the docs build and the `run-doctests` skill set it to `true` beside `set_compact_show!(false)`, so no jldoctest block moves. The selection has three arms with a fixed precedence: a per-name entry beats a type's own `show_fields` overload, which beats the global switch, and `pretty_show_fields` resolves them at both sites the macro used to read `fieldnames`. The per-name key is a `Symbol`, because the preference channel is TOML and carries no type, and an unknown name is accepted because a type outside the package can render through the macro. A type whose every field is hidden prints as `T()`. |
| 0106 | [A partial-fit state is the one Result an estimator holds](0106-a-partial-fit-state-is-the-one-result-an-estimator-holds.md) | | `CLAUDE.md` refuses a Result on an estimator without exception, and the incremental fit decided by #308 puts the running count, mean and second-moment accumulator in an optional field on the estimator. The exception rests on one property: a state is not consumable. No consumer of a moment, a prior or a risk measure reads one, and a read-out verb turns a state into the ordinary Result first, so a state never stands in for the answer. The enforcement stays the field's type bound: `Union{Nothing, <:AbstractPartialFitState}` admits one small unexported family and still refuses an arbitrary Result. The root sits under `AbstractResult`, so the length-1 iteration protocol and the pretty `show` come free and neither `Union` moves. Every state owes two methods, `merge_states` and `Base.copy`, with `assert_mergeable_states` refusing a pair of different types or over different numbers of assets and `chan_merge` carrying the mathematics once; ADR 0107 owns the `copy` rule. `Base.merge` was refused because its `Dict` and `NamedTuple` contract is that the right operand wins a key conflict, where this merge is a sum. `RegimeAdjustedVarianceCache` re-parents to the new root, which is the shipped instance of the shape. |
| 0107 | [The update seam has two verbs, and a view slices a state by asset and drops it by observation](0107-the-update-seam-has-two-verbs-and-a-view-slices-by-asset-and-drops-by-observation.md) | | ADR 0106 settled where a state lives and not what the verb that writes one promises, and the two shipped families answered differently: the second-order methods aliased the state, and the higher-moment methods built a fresh one. The shipped aliasing sentence was false in its detail anyway, because a kept estimator holds moved arrays and an old count. So the seam takes two verbs. `partial_fit!` is the method each family writes, it is that family's cheapest exact fold, and it promises nothing about a kept estimator; its docstring names the three pitfalls in place of the promise. `partial_fit` is one generic method with value semantics: it copies the state and folds the copy, which is the pair `matrix_processing` makes with `matrix_processing!`, and it restores the no-contamination reason #308 gave for `Accessors.@reset`. The higher-moment family overrides it, because its fold builds a fresh state either way and the generic copy would cost 800 MB of `M4` per call at a hundred assets. Both verbs are exported, and each state owes a `Base.copy` naming its own constructor. On the propagation channels the tags are `@fprop @vprop cache`: `factory` carries the state, `port_opt_view` slices it to the selected assets by index copy, and `obs_weights_view` drops it through one root method returning `nothing`. No new tag was needed. The slice copies rather than views, because a fold on a viewed estimator would otherwise write through into the arrays it was viewed from; it is exact, because a co-moment of `k` assets reads those assets' observations alone. The observation axis has no slice at all, since removing an observation from a Welford accumulator has no numerically stable inverse. Every state read-out runs the same configuration guard the fold runs, so an estimator whose `w` was replaced by `factory` refuses rather than answering from a state fitted unweighted. |
| 0109 | [A Universe Sets carries two factor axes, and the loadings result picks the one a consumer reads](0109-a-universe-sets-carries-two-factor-axes-and-the-loadings-result-picks-the-one-a-consumer-reads.md) | | `UniverseSets` declared one factor axis, and #706 and #707 gave the prior's loadings slot a second family whose factors are the exposures its own fit was built from. Those exposures reach no `ReturnsResult`, so one key would have to name two different lists on one problem, and a constraint resolved against the wrong list still assembles and still solves. So `fkey`/`ufkey` become `tfkey`/`utfkey` and `cfkey`/`ucfkey` join them, defaults `"ncf"`/`"ucf"`, validated by the same two rules written once and called twice, never validated against each other, and carried through a view alike. A consumer names its axis by dispatch: `factor_axis_key(sets, rr)` reads the key off the block that carries `M`, with three methods covering `RegE_Reg` and no fallback on the loadings root. An `axis` field per consumer was refused because it can disagree with the loadings and nothing can check it; reading whichever key `dict` carries was refused because a problem declaring both axes has no answer. `factor_universe` states its key, the Black-Litterman `axis` symbols widen to `(:xkey, :tfkey, :cfkey)`, and the optimiser's name check and `pipeline_asset_sets` stay on the time-series axis because `rd.nf` is that axis by definition. The rename is a hard break, as #705 was. |
| 0110 | [The two evaluation switches are separate, and the library does not bundle them](0110-the-two-evaluation-switches-are-separate-and-the-library-does-not-bundle-them.md) | | A backtest reads a fold in two places, and each place held one fixed answer: a fold's series was `X * w` net of fees, and the fold loop threaded the previous fold's target weights. A reference implementation answers both with one flag, which makes a drifted run sequential for a dependency the drift does not have. So the library splits them: `wd::Option{<:AbstractWeightDrift}` on the four schemes that can read it, `pws::Option{<:AbstractPreviousWeightsSource}` on the two walk-forwards alone, `nothing` on either meaning today's behaviour. The drift is a cumulative product, the held weights are one step beyond the last row of the path, and a drifted fold carries a `HeldWeightsResult` read by dispatch. `needs_previous_weights` does not move, so a drifted run stays parallel; four settings exist where the reference has two; a turnover cap binds the trade only when the source is on. The fee's clock is a third, orthogonal switch, `fa` on both fee types. |
| 0111 | [The ucs triple gains a prior arm, fitted from the optimisation's own prior result](0111-the-ucs-triple-gains-a-prior-arm-fitted-from-the-optimisations-own-prior-result.md) | | Every uncertainty set estimator carried its own `pe` and fitted it on the returns it was handed, but #643's orthogonal sets read the factor model of the prior the optimiser is solving on, and no returns matrix carries it. So an unexported `AbstractPriorUncertaintySetEstimator` declares `ucs`/`mu_ucs`/`sigma_ucs` over an `AbstractPriorResult`, and the triple gains a three-argument form whose three methods route each estimator to the argument it reads: the returns-data base drops the prior, the new root drops the returns, and the existing `args...` passthrough answers a built set. Both JuMP builders pass `pr` beside `rd`; the near-optimal-centering pre-fit passes the new root through unchanged, so each corner solve fits it against its own prior; the Pipeline gains one step method requiring the `:prior` slot; and the hierarchical route needs nothing, because the estimator passthrough of `port_opt_view` already carries it and the cluster refits from its own prior view. `Regression` gains an optional `esigma` with the cross-sectional block's name, shapes and guards, `factor_lift` returns the residual variances it already measured, and `FactorPrior` and `FactorBlackLittermanPrior` write them under `rsd = true` through `set_idiosyncratic_covariance`, which reads `L` and `b` with `getfield` because `Accessors.@set` would materialise `L` from the `swap(L, M)` rule. One reader, `idiosyncratic_variances`, answers off either block and refuses rather than inventing a weighting. `factory` was refused because it runs on no risk measure on the JuMP route; a Calibration Rule returns one number, and a Deferred Quantity refits from the returns. ADR 0050 is cited, not amended. |

ADRs 0017, 0018, and 0028 carry no `status` frontmatter. Every other ADR is `accepted`.

## Not an ADR

[`examples-coverage.md`](examples-coverage.md) is a living inventory of which pipeline-stage
topics have an example or a user-guide page. ADR 0014 records the decision behind its grouping.
