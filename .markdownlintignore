# Written by the docs build from `examples/**/*.jl`, `user_guide/*.jl` and
# `docs/capability_catalogue.jl`, and untracked (ADR 0151). A local build leaves them in the
# tree, and only the `.jl` source may be edited, so a lint error here cannot be fixed at the file
# it names.
docs/src/examples/
docs/src/user_guide/
docs/src/capability_catalogue.md
docs/src/TypeHierarchy.md

# Research notes. They are working documents, not repository documentation.
research/
